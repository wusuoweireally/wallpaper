# ============================================
# Docker Compose 专用环境变量
# 用于 docker-compose.prod.yml
# ============================================

# MySQL 密码（与 server/.env.production 中的 DB_PASSWORD 保持一致）
MYSQL_ROOT_PASSWORD=RainGem2024!Wallpaper
