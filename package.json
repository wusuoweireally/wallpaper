{
  "name": "wallpaper-system",
  "private": true,
  "version": "1.0.0",
  "description": "现代化的壁纸管理系统",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "type-check": "pnpm -r run type-check",
    "test": "pnpm -r run test",
    "docker:prod": "docker compose --env-file server/.env.production -f docker-compose.yml -f docker-compose.prod.yml up -d --build",
    "docker:prod:down": "docker compose --env-file server/.env.production -f docker-compose.yml -f docker-compose.prod.yml down",
    "docker:prod:logs": "docker compose --env-file server/.env.production -f docker-compose.yml -f docker-compose.prod.yml logs -f"
  },
  "packageManager": "pnpm@10.13.1",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"],
    "peerDependencyRules": {
      "allowedVersions": {
        "typescript": "7"
      }
    }
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "pnpm": ">=10.0.0"
  },
  "keywords": [
    "wallpaper",
    "vue3",
    "nestjs",
    "typescript",
    "wallpaper-system"
  ],
  "author": "",
  "license": "MIT"
}
