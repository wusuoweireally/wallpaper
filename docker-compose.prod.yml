# 生产环境 override；根 package.json 中的 docker:prod* 脚本会自动加载本文件。
services:
  mysql:
    restart: always

  server:
    restart: always
    environment:
      NODE_ENV: production

  web:
    restart: always
