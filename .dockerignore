.git
.claude
.tmp
**/.DS_Store
**/node_modules
**/dist
**/coverage
# 忽略所有真实 env 文件，避免密钥进入构建上下文/镜像（运行时由 compose env_file 注入）
**/.env*
web/dist
server/pnpm-lock.yaml
web/pnpm-lock.yaml
# 注意：不要忽略 server/uploads —— uploads/壁纸 是 demo-seed 的默认壁纸源图，必须打进镜像
